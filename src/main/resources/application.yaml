server:
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/myDatabase?useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: toxicjinx4551
  redis:
    host: 127.0.0.1
    port: 6379
    password: 123456
    jedis:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0
    timeout: 10000ms

mybatis:
  type-aliases-package: com.choi.elasticjob.pojo
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

